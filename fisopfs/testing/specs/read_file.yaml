name: Write/Read file
description: "Given a file on root folder writes and reads content"
given:
 - touch {fs}/file1
steps:
 - do: cat > {fs}/file1
   in: "uno"
   ret: 0
 - do: cat {fs}/file1
   out: "uno"
