name: Stat new file
description: "Creates a simple file on root folder. And gets stat of it."

steps:
 - do: touch {fs}/file1
   ret: 0
 - do: stat {fs}/file1
   path: "{fs}/file1"
   type: regular empty file
   size: 0
